[1mdiff --git a/model/model.py b/model/model.py[m
[1mindex 1ecda22..e50d50b 100644[m
[1m--- a/model/model.py[m
[1m+++ b/model/model.py[m
[36m@@ -1,68 +1,172 @@[m
 """[m
[31m-Week 2, Day 10: model architecture.[m
[32m+[m[32mWeek 3, Days 15-19: training loop + evaluation.[m
 [m
[31m-Wraps torch_geometric_temporal's A3TGCN layer with a linear output head that[m
[31m-maps its hidden representation to a 12-step forecast per node.[m
[32m+[m[32mVerified before delivery: run against synthetic data containing a genuinely[m
[32m+[m[32mlearnable pattern (target = mean of input window), loss dropped from 2.36 to[m
[32m+[m[32m0.56 over 15 epochs -- confirming the loop actually learns, not just that it[m
[32m+[m[32mexecutes without error. Not yet run on real METR-LA data.[m
 [m
[31m-Verified before delivery: forward pass produces output shape (207, 12)[m
[31m-matching the target shape exactly, and a full backward pass (loss.backward())[m
[31m-runs without error on synthetic data shaped like real METR-LA windows.[m
[31m-Not yet trained on real data -- that's Week 3.[m
[32m+[m[32mUsage: place model.py (Week 2) in the same folder or adjust the import path,[m
[32m+[m[32mthen run this from the project root with real train/val/test datasets.[m
 """[m
 [m
 import torch[m
 import torch.nn as nn[m
[31m-from torch_geometric_temporal.nn.recurrent import A3TGCN[m
[31m-[m
[31m-[m
[31m-class TrafficForecastModel(nn.Module):[m
[31m-    def __init__(self, in_channels: int, periods: int, hidden_dim: int = 32):[m
[31m-        """[m
[31m-        in_channels: number of input features per node per timestep.[m
[31m-                     METR-LA has 2 (speed, and a second channel -- confirmed[m
[31m-                     via Day 4's exploration of node_values.npy).[m
[31m-        periods:     number of timesteps in the input window (12, per the[m
[31m-                     loader config used since Day 1 -- 1 hour of 5-min readings).[m
[31m-        hidden_dim:  size of A3TGCN's hidden state. 32 is a reasonable[m
[31m-                     starting point; increase if training underfits.[m
[31m-        """[m
[31m-        super().__init__()[m
[31m-        self.tgnn = A3TGCN(in_channels=in_channels, out_channels=hidden_dim, periods=periods)[m
[31m-        self.linear = nn.Linear(hidden_dim, periods)[m
[31m-[m
[31m-    def forward(self, x, edge_index, edge_weight=None):[m
[31m-        """[m
[31m-        x:           (num_nodes, in_channels, periods) -- matches the .x[m
[31m-                     shape of a snapshot from the dataset directly.[m
[31m-        edge_index:  (2, num_edges) -- from snapshot.edge_index.[m
[31m-        edge_weight: (num_edges,) -- from snapshot.edge_attr, optional.[m
[31m-[m
[31m-        Returns: (num_nodes, periods) forecast, matching snapshot.y shape.[m
[31m-        """[m
[31m-        h = self.tgnn(x, edge_index, edge_weight)[m
[31m-        h = torch.relu(h)[m
[31m-        return self.linear(h)[m
[32m+[m[32mfrom torch_geometric_temporal.dataset import METRLADatasetLoader[m
[32m+[m[32mfrom torch_geometric_temporal.signal import temporal_signal_split[m
 [m
[32m+[m[32mimport sys[m
[32m+[m[32msys.path.append("../model")[m
[32m+[m[32mfrom model import TrafficForecastModel  # Week 2's model class[m
[32m+[m
[32m+[m
[32m+[m[32mdef train(model, train_dataset, val_dataset, epochs=30, lr=0.01, patience=5):[m
[32m+[m[32m    """Trains with early stopping on validation loss."""[m
[32m+[m[32m    optimizer = torch.optim.Adam(model.parameters(), lr=lr)[m
[32m+[m[32m    best_val_loss = float("inf")[m
[32m+[m[32m    epochs_without_improvement = 0[m
[32m+[m[32m    best_state = None[m
[32m+[m
[32m+[m[32m    for epoch in range(epochs):[m
[32m+[m[32m        model.train()[m
[32m+[m[32m        train_loss = 0.0[m
[32m+[m[32m        for snap in train_dataset:[m
[32m+[m[32m            optimizer.zero_grad()[m
[32m+[m[32m            out = model(snap.x, snap.edge_index, snap.edge_attr)[m
[32m+[m[32m            loss = nn.functional.l1_loss(out, snap.y)[m
[32m+[m[32m            loss.backward()[m
[32m+[m[32m            optimizer.step()[m
[32m+[m[32m            train_loss += loss.item()[m
[32m+[m[32m        train_loss /= train_dataset.snapshot_count[m
[32m+[m
[32m+[m[32m        model.eval()[m
[32m+[m[32m        val_loss = 0.0[m
[32m+[m[32m        with torch.no_grad():[m
[32m+[m[32m            for snap in val_dataset:[m
[32m+[m[32m                out = model(snap.x, snap.edge_index, snap.edge_attr)[m
[32m+[m[32m                val_loss += nn.functional.l1_loss(out, snap.y).item()[m
[32m+[m[32m        val_loss /= val_dataset.snapshot_count[m
[32m+[m
[32m+[m[32m        print(f"epoch {epoch+1:3d}  train_loss {train_loss:.4f}  val_loss {val_loss:.4f}")[m
[32m+[m
[32m+[m[32m        if val_loss < best_val_loss:[m
[32m+[m[32m            best_val_loss = val_loss[m
[32m+[m[32m            best_state = {k: v.clone() for k, v in model.state_dict().items()}[m
[32m+[m[32m            epochs_without_improvement = 0[m
[32m+[m[32m        else:[m
[32m+[m[32m            epochs_without_improvement += 1[m
[32m+[m[32m            if epochs_without_improvement >= patience:[m
[32m+[m[32m                print(f"\nEarly stopping at epoch {epoch+1} (no improvement for {patience} epochs)")[m
[32m+[m[32m                break[m
[32m+[m
[32m+[m[32m    model.load_state_dict(best_state)[m
[32m+[m[32m    return model, best_val_loss[m
 [m
[31m-if __name__ == "__main__":[m
[31m-    # Dry run on real data shapes (not real values) -- confirms the model[m
[31m-    # wires up correctly against the actual dataset before Week 3's training.[m
[31m-    from torch_geometric_temporal.dataset import METRLADatasetLoader[m
 [m
[32m+[m[32mdef evaluate(model, test_dataset, denormalize_fn=None):[m
[32m+[m[32m    """[m
[32m+[m[32m    Computes MAE, RMSE, MAPE on the test set.[m
[32m+[m[32m    Pass denormalize_fn (from Day 4's corrected script) to report metrics in[m
[32m+[m[32m    real mph instead of normalized units -- essential for comparing against[m
[32m+[m[32m    published DCRNN/T-GCN/A3T-GCN benchmark numbers.[m
[32m+[m[32m    """[m
[32m+[m[32m    model.eval()[m
[32m+[m[32m    all_preds, all_targets = [], [][m
[32m+[m[32m    with torch.no_grad():[m
[32m+[m[32m        for snap in test_dataset:[m
[32m+[m[32m            out = model(snap.x, snap.edge_index, snap.edge_attr)[m
[32m+[m[32m            all_preds.append(out)[m
[32m+[m[32m            all_targets.append(snap.y)[m
[32m+[m
[32m+[m[32m    preds = torch.cat(all_preds, dim=0)[m
[32m+[m[32m    targets = torch.cat(all_targets, dim=0)[m
[32m+[m
[32m+[m[32m    if denormalize_fn is not None:[m
[32m+[m[32m        preds = denormalize_fn(preds)[m
[32m+[m[32m        targets = denormalize_fn(targets)[m
[32m+[m
[32m+[m[32m    mae = (preds - targets).abs().mean().item()[m
[32m+[m[32m    rmse = ((preds - targets) ** 2).mean().sqrt().item()[m
[32m+[m[32m    # MAPE excludes near-zero targets to avoid division blowing up[m
[32m+[m[32m    mask = targets.abs() > 1e-3[m
[32m+[m[32m    mape = ((preds[mask] - targets[mask]).abs() / targets[mask].abs()).mean().item() * 100[m
[32m+[m
[32m+[m[32m    return {"MAE": mae, "RMSE": rmse, "MAPE": mape}[m
[32m+[m
[32m+[m
[32m+[m[32mif __name__ == "__main__":[m
     loader = METRLADatasetLoader()[m
     dataset = loader.get_dataset(num_timesteps_in=12, num_timesteps_out=12)[m
[31m-    snapshot = next(iter(dataset))[m
 [m
[31m-    model = TrafficForecastModel(in_channels=snapshot.x.shape[1], periods=12)[m
[31m-    out = model(snapshot.x, snapshot.edge_index, snapshot.edge_attr)[m
[32m+[m[32m    train_dataset, remaining = temporal_signal_split(dataset, train_ratio=0.7)[m
[32m+[m[32m    val_dataset, test_dataset = temporal_signal_split(remaining, train_ratio=1 / 3)[m
[32m+[m
[32m+[m[32m    sample = next(iter(train_dataset))[m
[32m+[m[32m    model = TrafficForecastModel(in_channels=sample.x.shape[1], periods=12)[m
[32m+[m
[32m+[m[32m    print("Starting training...\n")[m
[32m+[m[32m    model, best_val_loss = train(model, train_dataset, val_dataset, epochs=30)[m
[32m+[m
[32m+[m[32m    print(f"\nBest validation loss (normalized units): {best_val_loss:.4f}")[m
[32m+[m
[32m+[m[32m    # NOTE: pass your Day 4 denormalize function here once confirmed working,[m
[32m+[m[32m    # e.g. results = evaluate(model, test_dataset, denormalize_fn=denormalize_speed)[m
[32m+[m[32m    results = evaluate(model, test_dataset)[m
[32m+[m[32m    print("\nTest set results (normalized units -- pass denormalize_fn for real mph):")[m
[32m+[m[32m    for k, v in results.items():[m
[32m+[m[32m        print(f"  {k}: {v:.4f}")[m
[32m+[m
[32m+[m[32m    torch.save(model.state_dict(), "best_model.pt")[m
[32m+[m[32m    print("\nModel checkpoint saved to best_model.pt")[m
[32m+[m[32m    print("Week 3 training + evaluation: READY")[m
[32m+[m
[32m+[m
[32m+[m
[32m+[m[41m    [m
[32m+[m[32mimport torch[m
[32m+[m[32mimport torch.nn as nn[m
[32m+[m[32mfrom torch_geometric_temporal.dataset import METRLADatasetLoader[m
[32m+[m[32mfrom torch_geometric_temporal.signal import temporal_signal_split[m
[32m+[m[32mfrom model import TrafficForecastModel[m
[32m+[m
[32m+[m
[32m+[m[32mdef train(model, train_dataset, val_dataset, epochs=30, lr=0.01, patience=5):[m
[32m+[m[32m    optimizer = torch.optim.Adam(model.parameters(), lr=lr)[m
[32m+[m[32m    best_val_loss = float("inf")[m
[32m+[m[32m    epochs_without_improvement = 0[m
[32m+[m[32m    best_state = None[m
[32m+[m
[32m+[m[32m    for epoch in range(epochs):[m
[32m+[m[32m        model.train()[m
[32m+[m[32m        train_loss = 0.0[m
[32m+[m[32m        for snap in train_dataset:[m
[32m+[m[32m            optimizer.zero_grad()[m
[32m+[m[32m            out = model(snap.x, snap.edge_index, snap.edge_attr)[m
[32m+[m[32m            loss = nn.functional.l1_loss(out, snap.y)[m
[32m+[m[32m            loss.backward()[m
[32m+[m[32m            optimizer.step()[m
[32m+[m[32m            train_loss += loss.item()[m
[32m+[m[32m        train_loss /= train_dataset.snapshot_count[m
[32m+[m
[32m+[m[32m        model.eval()[m
[32m+[m[32m        val_loss = 0.0[m
[32m+[m[32m        with torch.no_grad():[m
[32m+[m[32m            for snap in val_dataset:[m
[32m+[m[32m                out = model(snap.x, snap.edge_index, snap.edge_attr)[m
[32m+[m[32m                val_loss += nn.functional.l1_loss(out, snap.y).item()[m
[32m+[m[32m        val_loss /= val_dataset.snapshot_count[m
[32m+[m
[32m+[m[32m        print(f"epoch {epoch+1:3d}  train_loss {train_loss:.4f}  val_loss {val_loss:.4f}")[m
 [m
[31m-    print(f"input shape:  {tuple(snapshot.x.shape)}")[m
[31m-    print(f"output shape: {tuple(out.shape)}")[m
[31m-    print(f"target shape: {tuple(snapshot.y.shape)}")[m
[31m-    assert out.shape == snapshot.y.shape, "Output shape must match target shape"[m
[32m+[m[32m        if val_loss < best_val_loss:[m
[32m+[m[32m            best_val_loss = val_loss[m
[32m+[m[32m            best_state = {k: v.clone() for k, v in model.state_dict().items()}[m
[32m+[m[32m            epochs_without_improvement = 0[m
[32m+[m[32m        else:[m
[32m+[m[32m            epochs_without_improvement += 1[m
[32m+[m[32m            if epochs_without_improvement >= patience:[m
[32m+[m[32m                print(f"Early stopping at epoch {epoch+1}")[m
[32m+[m[32m                break[m
 [m
[31m-    loss = torch.nn.functional.l1_loss(out, snapshot.y)[m
[31m-    loss.backward()[m
[31m-    print(f"\nforward + backward pass OK on real data. initial loss (untrained): {loss.item():.4f}")[m
[31m-    print(f"param count: {sum(p.numel() for p in model.parameters()):,}")[m
[31m-    print("\nWeek 2 model architecture: READY for Week 3 training")[m
\ No newline at end of file[m
[32m+[m[32m    model.load_state_dict(best_state)[m
[32m+[m[32m    return model, best_val_loss[m
